import React from 'react';
import { View, StyleSheet, Text } from 'react-native';

const Form = () => {
    return (
        <View style={styles.container}>
            <Text>Form section works!</Text>
        </View>
    )
}

export default Form;

const styles = StyleSheet.create({
    container: {}
})